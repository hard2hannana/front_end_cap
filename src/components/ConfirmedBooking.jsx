// src/ConfirmedBooking.jsx
export default function ConfirmedBooking() {
  return (
    <main
      className="container"
      role="main"
      aria-labelledby="booking-confirmed-title"
    >
      <h1 id="booking-confirmed-title">Booking confirmed 🎉</h1>
      <p>
        Thanks! Your reservation is locked in. We’ve sent a confirmation to your
        email.
      </p>
    </main>
  );
}
